We are currently working on a way to detect and prevent compromises on a system by leveraging ML and DL techniques for detection.

## UI improvements
The application now features a reusable dark theme. Colours are defined in `AllFunctions` and applied to all forms on load so the interface looks consistent and modern.
